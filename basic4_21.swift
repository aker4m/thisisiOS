let hasOther = true
let hasSmall = true
var defaultSize = 100
var userSize = 95

if (defaultSize >= userSize) || (hasOther && hasSmall){
    print("There is your size.")
} else{
    print("There isn't your size.")
}
